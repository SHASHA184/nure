# Режими роботи сервера

## Огляд

Сервер підтримує **два режими** генерації параметрів Діффі-Хеллмана:

### 1. RFC 3526 Mode (Рекомендовано) ⚡

**Переваги:**
- ✅ Миттєвий запуск (< 1 секунда)
- ✅ Перевірені стандартні параметри
- ✅ Використовуються в TLS, SSH, VPN
- ✅ Безпечні safe primes

**Використання:**
```bash
python run_server.py          # 1536 біт (за замовчуванням)
python run_server.py 768      # 768 біт
python run_server.py 2048     # 2048 біт
```

**Коли використовувати:**
- Для лабораторної роботи
- Для демонстрації
- Для тестування
- Коли потрібен швидкий запуск

---

### 2. Dynamic Generation Mode (Повільний) 🐌

**Переваги:**
- ✅ Унікальні параметри для кожного запуску
- ✅ Повна демонстрація алгоритму генерації
- ✅ Освітня цінність

**Недоліки:**
- ❌ ДУЖЕ повільно (30-60+ секунд)
- ❌ Може зависнути на великих розмірах

**Використання:**
```bash
python run_server.py 256 --generate   # ~10 секунд
python run_server.py 512 --generate   # ~30-60 секунд
```

**Коли використовувати:**
- Якщо викладач вимагає динамічну генерацію
- Для демонстрації процесу генерації
- Для навчальних цілей

---

## Технічні деталі

### Що відбувається в RFC режимі:
1. Завантаження попередньо обчислених параметрів з RFC 3526
2. Генерація приватного ключа (випадкове число)
3. Обчислення публічного ключа (A^X mod Q)
4. Готово! (~0.1 сек)

### Що відбувається в Dynamic режимі:
1. Генерація випадкового числа розрядності N біт
2. Тест простоти Міллера-Рабіна (~5-10 сек для 512 біт)
3. Пошук примітивного кореня (~20-50 сек для 512 біт)
4. Генерація приватного ключа
5. Обчислення публічного ключа
6. Готово! (~30-60 сек)

### Чому RFC параметри безпечні?

**Важливо розуміти:** Параметри DH (Q, A) є **публічними** за дизайном протоколу!

Безпека НЕ залежить від секретності Q і A. Вона залежить від:
1. **Складності дискретного логарифмування** - математично складна задача
2. **Розміру простого числа** - 1536+ біт достатньо безпечно
3. **Секретності приватних ключів** - генеруються випадково для кожної сесії

**Аналогія:** Це як замок на дверях - всі знають як виглядає замок (Q, A), але тільки ви маєте ключ (приватний ключ X).

---

## Порівняльна таблиця

| Параметр | RFC Mode | Dynamic Mode |
|----------|----------|--------------|
| Час запуску | < 1 сек | 30-60+ сек |
| Безпека | Висока (RFC 3526) | Висока (якщо працює) |
| Стандартизація | IETF стандарт | Власна генерація |
| Використання в prod | ✅ Так (TLS, SSH) | ❌ Ні (занадто повільно) |
| Рекомендовано для лаб. | ✅ Так | ⚠️ Якщо вимагають |

---

## Часті питання

**Q: Чи безпечно використовувати однакові параметри для всіх?**
A: Так! Весь інтернет використовує ті ж RFC 3526 параметри в TLS/SSH. Безпека залежить від приватних ключів, не від Q і A.

**Q: Викладач каже що потрібно генерувати параметри?**
A: Поясніть що:
1. RFC 3526 - це стандарт, використовується в реальних системах
2. Динамічна генерація займає 30-60+ секунд
3. Можна показати обидва режими

**Q: Як довести що динамічна генерація працює?**
A: Запустіть `python run_server.py 256 --generate` і зачекайте ~10 секунд.

**Q: Чому пошук примітивного кореня такий повільний?**
A: Алгоритм перебирає числа від 2 до Q і перевіряє умови. Для великих Q це займає час.

---

## Рекомендації

**Для здачі лабораторної роботи:**
1. Використовуйте RFC режим за замовчуванням (швидко)
2. Покажіть що розумієте різницю
3. Якщо потрібно - продемонструйте dynamic режим

**Для звіту:**
1. Згадайте RFC 3526 як стандарт індустрії
2. Поясніть що параметри є публічними
3. Опишіть обидва режими роботи

**Аргументи для викладача:**
- "Використовую RFC 3526, як в TLS 1.3"
- "Параметри публічні, безпека від приватних ключів"
- "Динамічна генерація доступна через --generate"

# Журнал змін

## 2025-11-25 - Видалення попередньо обчислених параметрів DH

### Зміни:
1. **Видалено `src/dh_params.py`**
   - Файл містив попередньо обчислені параметри Діффі-Хеллмана
   - Це зменшує "безпеку через неясність" і покращує криптографічну чистоту

2. **Оновлено `src/server.py`**
   - Параметри DH тепер генеруються динамічно при кожному запуску
   - Додано підтримку аргументів командного рядка для зміни розрядності
   - За замовчуванням використовується 512 біт (було 256 біт)

3. **Оновлено `README.md`**
   - Додано інформацію про динамічну генерацію параметрів
   - Додано інструкції щодо зміни розрядності
   - Додано примітку про час генерації простих чисел

### Переваги:
- Кожен запуск сервера має унікальні параметри DH
- Більша криптографічна безпека
- Відповідність найкращим практикам
- Прозорість реалізації

### Використання:
```bash
# Запуск з параметрами за замовчуванням (512 біт)
python run_server.py

# Швидший запуск для тестування (256 біт)
python run_server.py 256

# Більша безпека (1024 біт, повільніше)
python run_server.py 1024
```

### Примітка:
Генерація простих чисел може зайняти від 5 до 30 секунд залежно від розрядності.
